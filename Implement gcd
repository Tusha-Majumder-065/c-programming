#include <stdio.h>
void main(){
    int a,b,i,gcd;
    scanf("%d %d",&a,&b);
      printf("Enter two number:");
int c=(a < b) ? a:b;
for(int i=1;i<=c;i++){
    if(a % i == 0 && b % i == 0){
gcd =i;
}
}
printf("GCD = %d",gcd);
}
